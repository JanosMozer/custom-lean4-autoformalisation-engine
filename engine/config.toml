# Lean 4 Autoformalization Engine — configuration

title = "Fermat_Last_Theorem_n3"

problem = """
For all integers a, b, c and prime p >= 3, if p divides a*b*c and p does not divide a,
then there exist integers x, y such that a*x + p*y = 1.
"""

model_path = "path_to_model"

# Number of repair iterations (agent loop depth)
max_iters = 5

# Output format: "txt" | "yaml" | "json"
output_format = "txt"

# Output root; title is appended automatically
output_dir = "engine/formalised"

# Lean header prepended before each REPL check
lean_header = "import Mathlib\nopen BigOperators\n\n"

# Retrieval index paths (relative to workspace root)
retrieval_emb   = "retrieval/emb.npy"
retrieval_meta  = "retrieval/meta.jsonl"
retrieval_bm25  = "retrieval/bm25.json"

# System B: top-k premises injected into initial prompt
retrieval_k = 8

# Inference server socket
infer_host = "127.0.0.1"
infer_port = 9876

# Generation hyperparameters forwarded to Python server
temperature  = 0.7
max_new_tokens = 512
